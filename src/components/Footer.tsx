import { Contact } from './Contact';
import { Info } from './Info'

import '../styles/footer.css';

export const Footer = () => {
    return (
        <footer>
            <Contact />
            <Info />
        </footer>
    );
};
